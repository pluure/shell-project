#include <stdio.h>
/**
 * main - this is a simple betty style code
 * Return: always zero
 */
int main(void)
{
	int a, b, c, sum;

	b = 6;
	a = 13;
	c = 103;

	sum = a + b + c;
		printf("%d is the value of sum\n", sum);
	return (sum);


}
